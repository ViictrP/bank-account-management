description = "Retrieve data from datasource (db, 3rd party, etc...)"

dependencies {
    implementation project(":application:core")
    implementation libs.database

    testImplementation libs.unit_tests
}
