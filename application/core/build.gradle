description = "Entities (domain objects) and uses cases that implement the business logic"

dependencies {
    implementation libs.string_utils
    implementation libs.log
    testImplementation libs.unit_tests
}